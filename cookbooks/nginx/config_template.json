worker_processes 1;

events {
  worker_connections 1024;
}

http {
  include mime.types;
  default_type application/octet-stream;
  sendfile on;
  keepalive_timeout 65;

  passenger_root /Users/camwest/.rvm/gems/ruby-1.8.7-p352/gems/passenger-3.0.9;
  passenger_ruby /Users/camwest/.rvm/bin/passenger_ruby;

  server {
    listen 8080;
    server_name *.woople.local;
    root /Users/camwest/Sites/woople/public;
    passenger_enabled on;
    rack_env development;
  }
}

